{
  "name": "ai-news-builder",
  "version": "1.0.0",
  "private": true,
  "description": "Fetch RSS feeds, deduplicate, classify AI news, and generate static JSON",
  "main": "build.js",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "rss-parser": "^3.13.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
